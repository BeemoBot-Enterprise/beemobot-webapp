---
import { Image } from "astro:assets";
import teemoImage from "../assets/teemo-character.svg";
import { Button } from "./ui/button";
---

<section class="relative overflow-hidden py-24">
  <div class="container mx-auto px-6">
    <div class="flex flex-col md:flex-row items-center">
      <div class="md:w-1/2 mb-10 md:mb-0">
        <h1 class="text-4xl md:text-5xl font-bold mb-4">
          Le meilleur bot<br />
          discord Teemo<br />
          tout-en-un
        </h1>
        <p class="text-gray-300 mb-8 max-w-md">
          Teemo bot est un outil discord complet, mettant en relation les
          joueurs et offrant un contrôle puissant pour que la passion puisse
          gagner quoi!
        </p>
        <div class="flex space-x-4">
          <Button className="bg-blue-600 hover:bg-blue-700" client:load>
            Ajouter le bot
          </Button>
          <Button
            variant="outline"
            className="text-white border-white hover:bg-white/10"
            client:load
          >
            Documentation
          </Button>
        </div>
      </div>
      <div class="md:w-1/2 relative">
        <Image
          src={teemoImage}
          alt="Teemo Character"
          class="w-full max-w-md mx-auto"
        />
      </div>
    </div>
  </div>

  <!-- Gradient background effect -->
  <div
    class="absolute -bottom-24 -left-24 w-96 h-96 bg-yellow-500 opacity-20 rounded-full blur-3xl"
  >
  </div>
  <div
    class="absolute top-12 right-12 w-72 h-72 bg-orange-500 opacity-20 rounded-full blur-3xl"
  >
  </div>
</section>
